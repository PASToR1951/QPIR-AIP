server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml application/xml+rss text/javascript
               image/svg+xml;

    # Long-lived cache for hashed static assets (JS, CSS, fonts, images)
    location ~* \.(js|css|woff2?|ttf|eot|ico|png|jpg|jpeg|gif|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # index.html must never be cached — it references content-hashed chunk
    # filenames that change every deploy. A cached index.html would keep
    # pointing browsers at chunks that no longer exist (404 on dynamic import).
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        expires -1;
        try_files $uri =404;
    }

    # React Router — all routes fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
